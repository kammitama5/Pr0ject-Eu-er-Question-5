# for all values of n
# if n - 1 % == 0
# go n - 2 .... until n - (n - 1) => recursive

# set limit? eg values < 500 000
# find value n between 1 to 20 where 
# x (which represents value of x between 1 and 20)
# recursively stepped through until x - (x - 1) % == 0